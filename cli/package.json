{
  "name": "copilot-cli",
  "version": "0.1.0",
  "description": "Minimal GitHub Copilot CLI (device-code auth + chat) built from obsidian-github-copilot repo reference",
  "type": "module",
  "bin": {
    "copilot-cli": "./bin/copilot-cli.js"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pkg": "^5.8.1"
  }
  ,
  "scripts": {
    "build:win": "pkg ./bin/copilot-run.cjs --targets node18-win-x64 -o ./dist/copilot-cli.exe",
    "build:linux": "pkg ./bin/copilot-run.cjs --targets node18-linux-x64 -o ./dist/copilot-cli",
    "build:all": "npm run build:linux && npm run build:win"
  }
}
